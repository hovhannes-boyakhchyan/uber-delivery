export enum Statuses {
  pending = 'pending',
  pickup = 'pickup',
  pickup_complete = 'pickup_complete',
  dropoff = 'dropoff',
  delivered = 'delivered',
  canceled = 'canceled',
  returned = 'returned',
  ongoing = 'ongoing',
}
